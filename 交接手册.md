# Lico LeetCode 辅导应用 · 交接手册

> 生成日期：2026-07-29
> 适用副本：`C:\Users\14481\Downloads\lico-leetcode-agent-main (2)\lico-leetcode-agent-main`（**真正在跑 + 真正在改的版本**）
> 配套记忆：`(2)/.workbuddy/memory/2026-07-29.md`（按天记录，含每段改动的根因/代码落点）

---

## 〇、给接手同学的一句话

这是一只"上发条的小蜜蜂"力扣手撕辅导应用：前端原生 JS、后端 FastAPI + SQLite，本地双击 `start.bat` 即可跑起来，把"听题 → 讲解 → 抄写 → 默写"的学习闭环做成有语音、有轨迹的辅导体验。**所有开发改动都发生在 (2) 副本**，git 仓库在 (1)。请先读完下面的"铁律"与"副本说明"，能省掉 90% 的返工。

---

## 一、副本与运行说明（务必先看）

机器上有**多份** Lico 副本，历史上反复搞混，现明确如下：

| 路径 | 角色 |
|---|---|
| `…\lico-leetcode-agent-main` | 最老原版（基本弃用） |
| `…\lico-leetcode-agent-main (1)\lico-leetcode-agent-main` | **git 仓库**（`zouchuheiye/lico-leetcode-agent`，GitHub `main` 分支）。由 (2) 同步而来，是代码的"发布/存档真源"。 |
| `…\lico-leetcode-agent-main (2)\lico-leetcode-agent-main` | **开发真源（当前活跃版）**：所有前端/后端改动都落在这里；`start.bat` 也在这里。 |
| `D:\lico_v2\` | 早期 v7 试作版（已弃用，勿碰） |

**铁律**：看到浏览器截图 / 被要求"加功能 / 修 bug"，一律去 **(2)** 改。改完若需归档，再 `cd (1) && git commit`（**不推送**，等用户确认）。

**启动**：在 (2) 双击 `start.bat`（内部由 `run.py` 起 FastAPI + uvicorn，`0.0.0.0:8000`；首次自动装依赖）。**改了后端必须重启**该进程，否则新端点会 404 / NOT FOUND。
- 杀端口：`netstat -ano | findstr :8000` 拿 PID → `Stop-Process -Id <pid> -Force`（父子 uvicorn 都要杀）。
- 如需向 GitHub 推送，代理 / 凭据由仓库所有者提供；接手人通常只需在 (1) 提交，推送由所有者执行。

---

## 二、系统架构

- **前端**：原生 JS（无框架），单页多 `screen`（`screenStartBox / screenLearn / screenArchive / screenRecall / screenFinish / screenModelConfig / screenConfig`）。状态机集中在 `app.js` 顶部 `state` 与 `showScreen(id)`（顶部按钮态的**唯一真相来源**）。
- **后端**：FastAPI + SQLite（`storage.py` 封装所有 DB 读写）。
- **关键约束**：所有写操作（`explain / steps / review / event / progress / done`）**都显式带 `problem_id`**，按 `storage.get_problem(problem_id)` 精确定位——这意味着"平行时空"在技术上零冲突（见下文）。
- **语音**：`bee.js` 用浏览器 `window.speechSynthesis`（Web Speech API），自动挑中文 voice（zh / Yaoyao / Huihui / Tingting），pitch=1.4（高音"小蜜蜂"感）、rate=1.0；逐字高亮靠 `onboundary` 事件校准。

---

## 三、已实现功能清单

### 1. 学习主流程（题目 → 举例 → 分步讲解 → 抄写 → 完成）
- 题目页：`onReadDone` 在"蜜蜂说话"的同时后台 `API.explain` 已并行启动（不等人说完才生成）。
- 讲解页：`onReadDone` 同理，说话同时后台 `API.steps` 并行。
- 抄写完成 `finishCopy`：蜜蜂说话与 `API.start`（生成下一题）并行。
- 顶部「🐝 去做新题」= 回学习页主出口；「📚 已做题集」「📝 默写板」= 两个"板"之间导航（由 `showScreen` 单一来源设置，切勿在点击处理里提前 set 按钮态，会被覆盖）。

### 2. 讲解页（逐段显现）
- 首段直接显现，其余段落 `.locked`（纯空白占位）。
- 每段后：「我懂了，显现下一段」+「🔊 听这一段」小喇叭。
- 最后一段按钮变「我懂了，继续 →」且此时才显示总听按钮。
- 修过"听讲解把分段弄丢"的 bug（`speakHL` 重构为保留 `<p>/<br>` 结构、高亮与朗读文本严格对齐）。

### 3. 抄写页
- 滚动改为智能跟随（仅快出视口才轻滚，不再抢手动滚动）。
- 点已抄完的行 → 回车进入下一行；「↺ 重抄」按钮重置到最初。
- 缩进：**每行从 0 开始**，无自动缩进记忆（产品决策：原自动缩进方案已撤销）。
- 抄写内容：点「下一步默写」时通过 `API.event(..., "step_copy", {copy})` 落库 `events` 表（后端零改动）。

### 4. 默写板（`screenRecall`，原"默写版"）
- 仿学习轨迹布局：左列题目列表（独立滚轮）+ 右列固定高度默写框（代码可内部滚动），提交/清空按钮贴底。
- 标题/文案统一为「默写板」（非"默写版"）。
- **实时草稿缓存** `recallDrafts[pid]`：切题、切页再回来内容不丢；「🧹 清空重来」同步清缓存。**（已知限制：仅会话级，刷新丢）**
- 提交：主线模式调 `API.review` 由审查小蜜蜂评判；平行时空模式仅本地、不落库。
- 进入默写板时仅 `btnRecall` 显示「🐝 去做新题」，`btnArchive` 保持「📚 已做题集」（只留一个"回学习页"出口，避免功能重复）。

### 5. 学习轨迹（`screenArchive`）
- 只展示 4 类核心事件：`problem_generated（题目）/ explanation_generated（举例）/ steps_generated（分步讲解）/ step_copy（抄写）`。其余噪音事件（listen/read 等）已删除不进轨迹。
- 左列题目列表固定高度 + 滚轮翻阅（不随题数撑高）。
- 复习轨迹可识别 `step_copy` 事件并显示抄写内容。
- **已删除**「回到这一题 / 重新学习 / 回到当前题」三个 relive 按钮及整套后端机制（该方案被需求方否决，见第六章替代方案）。

### 6. 平行时空线（Parallel Timeline）
- 概念：主线 = `get_current_problem()` 的 learning 题（永不因旧题被打扰）；平行时空 = 任意旧题以"穿越回过去"模式打开，**复用其已生成内容**复习。
- 入口：轨迹页每题「🕰 回到这一题（平行时空）」；点当前主线题时自动变「↩ 回到这一题（主线）」。
- 右上角浮动小按钮「⏱ 回到现在」一键回主线（蜂蜜色药丸，hover 显示当前复习的是哪题）。
- 平行模式：跳过 `done`、不进下一题；抄写 `step_copy` 仍落库（写回旧题记录）、敲码仅本地。
- 后端新增只读聚合端点 `GET /api/learn/problem/{id}`（零写入）。

### 7. 模型配置
- 「⚙️ 配置模型」独立页，相关记忆 `modelCfgReturnScreen` 与默写板互不踩。

---

## 四、接手人必看的"坑位"

1. **顶栏按钮态**只能由 `showScreen(id)` 设置。在点击处理里 `btnArchive.textContent=...` 再调会触发 `showScreen` 的函数 → 设置被覆盖（曾因此返工）。
2. **后端改完要重启** `start.bat`，否则新端点 404。
3. **多份副本**：编辑目标 = (2)，别改到 (1) 或 `D:\lico_v2`。
4. **草稿仅会话级**：抄写 `state.drafts`、默写 `recallDrafts` 刷新即丢（需求若变要升级 localStorage / 后端 draft 表）。
5. **`get_current_problem()` 保留**：仍被 `learn_start` 复用，删除 relive 时别误删它。
6. 语音高亮对齐靠 `onboundary`，部分浏览器该事件不准 → 已有兜底（超时强制结束、防卡流程）。

---

## 五、已知限制 / 待办

- [x] ~~(2) 的大量前端改动尚未同步进 (1) 的 git~~ —— 已通过 (2)→(1) 同步并提交（含 README 更新），再经所有者推送至 GitHub `main` 分支，现已上线。
- [ ] 抄写/默写草稿刷新丢失（会话级）。
- [ ] 学习轨迹无"正式重做旧题"入口（relive 整套被删）。学习者希望无阻碍回到任意旧题复习——目前靠平行时空曲线达成，但缺少"把旧题正式置为当前题"的轻量机制。
- [ ] 题目/轨迹列表随数量增长全量渲染 DOM（暂无虚拟滚动/分页）。
- [ ] 讲解/步骤 LLM 生成非流式，需等待整段返回。

---

## 六、优化方向

### A. 语音朗读 ⭐ 高优先（需求方明确要求的优化项）
当前用浏览器 Web Speech API，机械感重、断句怪、尖音（pitch 1.4）。建议：
1. **换云端 TTS**：接入腾讯云/微软 Azure/OpenAI/Edge TTS，音质、情感、停顿自然得多，音频可缓存复用（同一讲解不必每次合成）。
2. **参数调优 + 用户可调**：pitch 1.4 偏尖，建议默认 1.1~1.2；rate 对中文可降到 0.95；增加"语速/音调"设置面板，偏好存 localStorage。
3. **长文本分句朗读**：按标点智能切句、句间短暂停顿，支持「暂停 / 继续 / 重听 / 点句跳转」，加朗读进度条。
4. **代码朗读预处理**：变量名、驼峰、括号应拆词（`getMax`→"get Max"、`<`读作"小于"），否则机器人念成一串乱码——这点对算法题讲解尤其重要。
5. **语音选择与语言自动识别**：记忆用户偏好的 voice；题目是英文时自动切英文 voice，而非一律 zh-CN fallback。
6. **高亮对齐增强**：`onboundary` 在部分浏览器不准，可改用"按估算时长"驱动高亮，或分句级对齐。

### B. 学习体验增强 ⭐ 高优先
7. **默写/敲码智能评判可视化**：目前只给 severity+文字反馈，建议加「逐行 diff 高亮」「哪几行错了」「预期 vs 实际」对比视图。
8. **错题本 + 复习调度（艾宾浩斯）**：默写/敲码错的题自动归集；按正确率自动排复习提醒，而非单纯"全部旧题平铺列表"。
9. **学习数据看板**：今日题数、连续天数、掌握度曲线、各题投入时间。

### C. 平行时空补完
10. **正式"回到旧题"轻量机制**：加 `POST /api/learn/relive-lite`（或"放弃当前题"端点），把旧题置为当前题而不清空内容，解决"想认真重做某题"的需求；与"平行时空（只读复习）"形成两级。
11. **平行练习也入轨迹但折叠/标灰**，便于回顾而不污染"已学完"统计。

### D. 工程稳定性
12. **草稿持久化**：升级 localStorage 或后端 draft 表，刷新不丢（直接消掉已知限制）。
13. **启动自检**：前端加版本号接口 + 端点探活，后端改了没重启时给明确红字提示，而非让用户猜 404。
14. **副本收敛**：多份项目极易改错，建议收敛为一份，git 作唯一真源，补 README 说明"编辑目标 = (2)"。

### E. 性能与响应式
15. **列表虚拟滚动/分页**：题量上千后不卡。
16. **讲解/步骤流式输出（SSE）**：边生成边显示，降低等待焦虑。
17. **移动端适配**：当前为 desktop 布局，小屏仅有 `max-height:72vh` 兜底，需系统响应式。

### F. 模型配置
18. **预设 + 连接测试**：OpenAI/DeepSeek/本地 Ollama 一键预设，加"测试连接"按钮，避免配错 Key 跑到提交才报错。

---

*本手册与 `(2)/.workbuddy/memory/2026-07-29.md` 互为补充：日志是"改了什么 / 为什么"，本手册是"现在有什么 / 接下来怎么走"。终端使用者视角的说明见仓库根目录 `README.md`。*
